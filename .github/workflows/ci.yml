name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:
    inputs:
      run_infra_smoke:
        description: Run Cloudflare, OpenAI secret, and Hetzner SSH smoke checks
        required: true
        type: boolean
        default: false
      deploy_control_api_dev:
        description: Deploy the Cloudflare Control API dev Worker
        required: true
        type: boolean
        default: false
      run_ai_gateway_live_smoke:
        description: Deploy dev Worker with AI Gateway config and run a live LLM smoke
        required: true
        type: boolean
        default: false

permissions:
  contents: read

jobs:
  contracts:
    name: Contract tests
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install development dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"

      - name: Run contract tests
        run: python -m pytest

      - name: Run linting
        run: python -m ruff check .

      - name: Validate JSON files
        run: |
          python3 - <<'PY'
          import json
          from pathlib import Path

          for path in Path(".").rglob("*.json"):
              json.loads(path.read_text(encoding="utf-8"))
          PY

  worker:
    name: Cloudflare Worker
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm

      - name: Install Worker dependencies
        run: npm ci

      - name: Generate Worker binding types
        run: npm run worker:types

      - name: Check generated Worker types are committed
        run: git diff --exit-code workers/control-api/src/worker-configuration.d.ts

      - name: Type-check Worker
        run: npm run worker:typecheck

      - name: Run Worker tests
        run: npm run worker:test

      - name: Dry-run Worker deploy
        run: npm run worker:check

  deploy-control-api-dev:
    name: Deploy Cloudflare Control API dev
    runs-on: ubuntu-latest
    needs: worker
    if: |
      github.event_name == 'workflow_dispatch' &&
      (inputs.deploy_control_api_dev == true || inputs.run_ai_gateway_live_smoke == true)

    env:
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CONTROL_API_TOKEN: ${{ secrets.CONTROL_API_TOKEN }}
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      AI_GATEWAY_ID: ${{ vars.AI_GATEWAY_ID }}
      SCAS_CONTROL_API_URL: https://scas-control-api-dev.still-butterfly-bbff.workers.dev

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm

      - name: Install Worker dependencies
        run: npm ci

      - name: Generate Worker binding types
        run: npm run worker:types

      - name: Type-check Worker
        run: npm run worker:typecheck

      - name: Run Worker tests
        run: npm run worker:test

      - name: Dry-run Worker deploy
        run: npm run worker:check

      - name: Configure dev Worker secrets
        shell: bash
        run: |
          set -euo pipefail

          if [ -z "${CONTROL_API_TOKEN:-}" ]; then
            echo "::error::Missing required secret: CONTROL_API_TOKEN"
            exit 1
          fi
          if [ -z "${OPENAI_API_KEY:-}" ]; then
            echo "::error::Missing required secret: OPENAI_API_KEY"
            exit 1
          fi

          printf '%s' "${CONTROL_API_TOKEN}" \
            | npx wrangler secret put CONTROL_API_TOKEN --config workers/control-api/wrangler.toml
          printf '%s' "${OPENAI_API_KEY}" \
            | npx wrangler secret put OPENAI_API_KEY --config workers/control-api/wrangler.toml

      - name: Configure AI Gateway dev vars
        shell: bash
        run: |
          set -euo pipefail

          if [ -z "${CLOUDFLARE_ACCOUNT_ID:-}" ]; then
            echo "::error::Missing required secret: CLOUDFLARE_ACCOUNT_ID"
            exit 1
          fi
          python - <<'PY'
          import os
          import re
          from pathlib import Path

          config = Path("workers/control-api/wrangler.toml")
          text = config.read_text(encoding="utf-8")
          account_id = os.environ["CLOUDFLARE_ACCOUNT_ID"].strip()
          gateway_id = os.environ.get("AI_GATEWAY_ID", "").strip() or "default"
          text = re.sub(
              r'AI_GATEWAY_ACCOUNT_ID = "[^"]*"',
              f'AI_GATEWAY_ACCOUNT_ID = "{account_id}"',
              text,
          )
          text = re.sub(
              r'AI_GATEWAY_ID = "[^"]*"',
              f'AI_GATEWAY_ID = "{gateway_id}"',
              text,
          )
          config.write_text(text, encoding="utf-8")
          PY

      - name: Deploy dev Worker
        run: npm run worker:deploy:dev

      - name: Run AI Gateway live smoke
        if: inputs.run_ai_gateway_live_smoke == true
        env:
          SCAS_CONTROL_API_TOKEN: ${{ secrets.CONTROL_API_TOKEN }}
        run: |
          python3 scripts/cloudflare/ai_gateway_live_smoke.py \
            --control-api-url "${SCAS_CONTROL_API_URL}"

  infrastructure-smoke:
    name: Infrastructure smoke test
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' && inputs.run_infra_smoke == true

    env:
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ZONE_ID: ${{ secrets.CLOUDFLARE_ZONE_ID }}
      HETZNER_HOST: ${{ secrets.HETZNER_HOST }}
      HETZNER_SSH_KEY: ${{ secrets.HETZNER_SSH_KEY }}
      HETZNER_USER: ${{ secrets.HETZNER_USER }}
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      CONTROL_API_TOKEN: ${{ secrets.CONTROL_API_TOKEN }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Check required secrets are present
        shell: bash
        run: |
          set -euo pipefail

          missing=0
          for name in \
            CLOUDFLARE_ACCOUNT_ID \
            CLOUDFLARE_API_TOKEN \
            CLOUDFLARE_ZONE_ID \
            HETZNER_HOST \
            HETZNER_SSH_KEY \
            HETZNER_USER \
            OPENAI_API_KEY \
            CONTROL_API_TOKEN
          do
            if [ -z "${!name:-}" ]; then
              echo "::error::Missing required secret: ${name}"
              missing=1
            fi
          done

          exit "${missing}"

      - name: Check Cloudflare API token
        shell: bash
        run: |
          set -euo pipefail

          curl --fail --silent --show-error \
            --header "Authorization: Bearer ${CLOUDFLARE_API_TOKEN}" \
            "https://api.cloudflare.com/client/v4/user/tokens/verify" \
            >/dev/null

      - name: Check OpenAI secret is present
        shell: bash
        run: |
          set -euo pipefail
          test -n "${OPENAI_API_KEY}"

      - name: Check Hetzner SSH access
        shell: bash
        run: |
          set -euo pipefail

          install -m 700 -d "${HOME}/.ssh"
          printf '%s\n' "${HETZNER_SSH_KEY}" | tr -d '\r' > "${HOME}/.ssh/hetzner_key"
          chmod 600 "${HOME}/.ssh/hetzner_key"

          if ! ssh-keygen -y -f "${HOME}/.ssh/hetzner_key" >/dev/null; then
            echo "::error::HETZNER_SSH_KEY must contain the complete private OpenSSH key block."
            echo "::error::Expected first line: -----BEGIN OPENSSH PRIVATE KEY-----"
            exit 1
          fi

          ssh-keyscan -H "${HETZNER_HOST}" >> "${HOME}/.ssh/known_hosts"

          ssh \
            -i "${HOME}/.ssh/hetzner_key" \
            -o BatchMode=yes \
            -o ConnectTimeout=10 \
            -o StrictHostKeyChecking=yes \
            "${HETZNER_USER}@${HETZNER_HOST}" \
            "printf scas-ssh-ok"
